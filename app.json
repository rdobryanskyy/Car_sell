{
  "name": "car_sell",
  "scripts": {
  },
  "env": {
    "BUILDPACK_URL": {
      "required": true
    },
    "DISABLE_COLLECTSTATIC": {
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "git://github.com/heroku/heroku-buildpack-python.git"
    }
  ]
}
